# Soup-Kitchen-Recursive-Methods

New Idea/Concept: Shelter

Every week there is a clothing drive (recursive method) (base items =0)
Once items are depleted, drive closes (remove method from arrayList)
Donate method which provides more clothes for shelter (using add method from arrrayList)
We could add a recursive method for the adding clothes as well.
Create own class called Storage (but what would it do?)
Use compareTo, if a guest needs a particular item from shelter storage, to see availability.